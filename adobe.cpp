#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
typedef pair<int,int> pll;
typedef vector<int> vi;
typedef vector<long long int> vll;

vi transer(vi a,vi b,int n){
     int x=a[0];
     auto y=upper_bound(b.begin(),b.end(),x);
}

signed main(){
ios_base::sync_with_stdio(false);
cin.tie(NULL);
    int t = 1; 
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        vi a,b;
        for(int i=0;i<n;i++){
            int x;
            cin>>x;
            a[i]=b[i]=x;
        }
        sort(b.begin(),b.end());
        int count=0;
        while(a!=b){

        }

     
        
            
    }
    return 0;
}